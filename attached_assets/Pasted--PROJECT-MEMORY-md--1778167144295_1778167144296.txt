أنت الآن في مرحلة حرجة جدًا.

اقرأ أولًا:

PROJECT_MEMORY.md

واستخدمه بالكامل لتقليل استهلاك التوكنز.

ممنوع إعادة تحليل المشروع من الصفر.

ممنوع أي mock data
ممنوع أي حلول تجميلية سطحية
ممنوع كسر الكود الحالي
ممنوع إنشاء أنظمة جديدة من الصفر إذا كان يوجد نظام قائم يحتاج إصلاح

قم بإصلاح وتطوير النظام الحالي فقط.

1) مشكلة الخريطة في الصفحة الرئيسية (Critical)

الخريطة الحالية متجمدة.

المستخدم لا يستطيع:

التحريك
التكبير
التصغير
التفاعل

هذا غير مقبول.

افحص:

React Native Maps
أو أي مكتبة مستخدمة حاليا

أصلح:

pan gestures
zoom
touch events
provider markers
performance

يجب أن تصبح الخريطة interactive بالكامل.

2) استكمال Refund System بالكامل

المستخدم لا يستطيع طلب استرداد من صفحة الطلب.

نفذ:

زر طلب استرداد داخل صفحة الطلب
يعمل في أي مرحلة
حتى بعد انتهاء الخدمة

عند قبول الاسترداد:

خصم المبلغ من محفظة المزود
تحديث wallet ledger
إشعار المستخدم
إشعار المزود

أضف:

refund timeline
refund status tracking
refund history
3) فصل الحجز الفوري عن الحجز المجدول (Critical Marketplace Logic)

النظام الحالي خاطئ جدًا لأن:

الحجز الآن
والحجز المجدول
يعملان بنفس المنطق.

هذا غير صحيح.

إذا اختار العميل "الآن"

يجب:

البحث فقط عن مزودين متاحين الآن
قريبين جغرافيًا
online
available

إذا لا يوجد مزود:

اعرض رسالة حديثة:

"لا يوجد مزودين متاحين بالقرب منك الآن"

مع زر:

"جدولة موعد لاحق"

إذا اختار العميل "موعد لاحق"

يجب:

إظهار قائمة مزودين مناسبين
التحقق من availability calendar
التحقق من working hours
منع الحجز إذا الموعد محجوز مسبقًا
عند إنشاء الحجز المجدول

يصل للمزود:

طلب جديد

مع:

قبول
رفض

ومؤقت:

5 دقائق

إذا لم يرد:

auto expire

4) إضافة منطقة الخدمة للمزود

في إعدادات المزود:

أضف:

المدينة الأساسية
منطقة الخدمة

الافتراضي:

المنصورة / الدقهلية

ويتم استخدامها في:

matching
search
map filtering
5) مشكلة حفظ صورة البروفايل

صورة المستخدم والمزود لا يتم حفظها.

افحص:

upload
storage
db update
cache refresh

أصلح المشكلة بالكامل.

6) بطاقة دعوة صديق

بطاقة referral الحالية:

تخرج خارج الشاشة.

أصلح:

responsive width
spacing
overflow
mobile layout
7) مشكلة حالة المزود (Critical)

بعد تسجيل دخول المزود:

حالته تكون:

غير متاح

عند التفعيل:

ترجع تلقائيًا غير متاح

ومع ذلك:

يبقى ظاهر على الخريطة
ويستقبل طلبات

هذا خطأ خطير جدًا.

أصلح:

provider availability state
realtime sync
map visibility
booking eligibility

إذا غير متاح:

لا يظهر على الخريطة
لا يستقبل طلبات

8) إشعارات المزود (Critical)

إذا التطبيق مفتوح:

يجب ظهور in-app modal notification حديثة فيها:

تفاصيل الطلب
قبول
رفض

إذا التطبيق بالخلفية:

يجب ظهور:

push notification حقيقية في شريط الهاتف

مع:

قبول
رفض

وعند الضغط:

ينتقل لصفحة الطلبات.

9) مشكلة push notifications بعد APK

الإشعارات كانت تعمل سابقًا لكن بعد بناء APK توقفت.

افحص بالكامل:

Expo push tokens
Firebase configuration
Android manifest
notification channels
background handlers
release build config

أصلح:

user notifications
provider notifications
refund notifications
rating notifications
withdrawal notifications
10) مراجعة دورة الحياة بالكامل

راجع بالكامل:

التسجيل
onboarding
الحجز
الدفع
تنفيذ الخدمة
التتبع
الإشعارات
الاسترداد
التقييم
السحب
انتهاء الخدمة

قم باختبار جميع السيناريوهات end-to-end.

بعد الانتهاء

قم بتحديث:

PROJECT_MEMORY.md

بكل:

fixes
files
database changes
notification architecture
scheduling architecture
refund architecture
مهم جدًا

أي:

كود وهمي
تسكين مؤقت
partial fix
كسر للنظام الحالي
تجاهل للمشاكل

= فشل كامل للمهمة

نفذ كـ:

Principal Marketplace Engineer
Principal Mobile Engineer
Principal Backend Engineer
Principal DevOps Engineer

حتى يصبح التطبيق منطقي مثل التطبيقات الكبرى في نفس المجال.